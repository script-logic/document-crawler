services:
  crawler:
    build: .
    container_name: document-crawler
    volumes:
      - ./data:/app/data
      - ./db:/app/db
    environment:
      - DATABASE__PATH=/app/${DATABASE__PATH}
    env_file:
      - .env
    command: crawl
