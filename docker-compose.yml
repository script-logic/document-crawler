services:
  crawler:
    build: .
    container_name: document-crawler
    volumes:
      - ./data:/app/data
      - ./crawler.db:/app/crawler.db
    env_file:
      - .env
    command: crawl
    restart: unless-stopped
